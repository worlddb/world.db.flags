require 'hoe'
require './lib/worlddb/flags/version.rb'


Hoe.spec 'worlddb-flags' do
  
  self.version = WorldDb::Flags::VERSION
  
  self.summary = 'worlddb-flags - country flags (24x24, 32x32, 48x48, 64x64) bundled for reuse w/ asset pipeline'
  self.description = summary

  self.urls    = ['https://github.com/geraldb/world.db.flags']
  
  self.author  = 'Gerald Bauer'
  self.email   = 'opensport@googlegroups.com'
    
  self.readme_file  = 'README.md'
  self.history_file = 'History.md'
  
end

